# TEMPLATE REPO: change trigger to the block below once the repo is configured.
#
# on:
#   push:
#     branches: ["*"]
#   pull_request:

name: ci

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Add language setup, lint, build, and test steps here.
